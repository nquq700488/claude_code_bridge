# 指令执行规范 (Instruction Execution Specification)

> 本规范定义了接收用户指令后的完整思考、拆解、执行、验证流程。
> **所有任务必须严格遵守此规范**。

---

## 一、指令理解阶段 (Understanding)

### 1.1 读取与解析
- **完整阅读**用户原始指令，不遗漏任何限定词、量词、时间词。
- 识别**显式要求**（用户直接说出的）与**隐式要求**（领域常识、上下文推断）。
- 区分**指令类型**：
  - `question` - 询问/解释类，只需回答
  - `research` - 探索/调研类，只需产出信息
  - `implementation` - 编码/修改类，需要落地代码
  - `operation` - 执行命令/运维类，有副作用
  - `meta` - 关于会话本身（配置、记忆、规范）

### 1.2 识别核心产物
明确本次指令要求交付的**最终产物**：
- 文件？代码？命令输出？解释？建议？
- 产物的存放位置、命名、格式。

### 1.3 消除歧义
- 若关键信息缺失或有多种合理解读，**先提问后执行**（使用 AskUserQuestion 或直接询问）。
- 对"高风险、不可逆、影响共享状态"的操作，**必须先确认**。

### 1.4 明确约束边界
识别本次任务的：
- **范围边界**：只改哪些文件/模块，不越界。
- **非目标**：用户没要求的事情，不要顺手做（不添加多余功能、不做预期之外的重构）。
- **风格约束**：CLAUDE.md、项目规则、代码风格。

---

## 二、任务拆解阶段 (Decomposition)

### 2.1 拆解原则
- **单一职责**：每个子任务只做一件可独立完成、可验证的事。
- **粒度适中**：单步 5–30 分钟工作量；过大需再拆，过小需合并。
- **可验证**：每个子任务有明确的"完成条件"。
- **原子性**：中途失败不应导致半成品污染主干。

### 2.2 依赖分析
- 构造依赖图：哪些任务可并行，哪些必须串行。
- 标注**阻塞关系**：A 的输出是 B 的输入 ⇒ A→B 串行。
- 标注**并行机会**：独立的搜索、读取、调研 ⇒ 并行 tool call。

### 2.3 使用 TodoWrite
- 子任务数 ≥ 3 或涉及多文件/多步骤时，**必须**使用 TodoWrite 记录。
- 每个 todo 保持 `content`（祈使）和 `activeForm`（进行时）两种形态。
- **同一时刻只允许一个任务处于 `in_progress`**。

---

## 三、执行阶段 (Execution)

### 3.1 执行顺序原则
1. **先读后写**：修改前必读当前状态。
2. **先探后改**：不确定代码结构时先调研再动手。
3. **先小后大**：先做可验证的小闭环，再扩展。
4. **依赖优先**：被依赖的任务先做。
5. **可逆优先**：把不可逆操作放到最后，给自己纠错空间。

### 3.2 工具选择
| 场景 | 首选工具 |
|------|---------|
| 读文件 | Read（不要 cat） |
| 改文件 | Edit（不要 sed） |
| 新建文件 | Write |
| 多处探索 | Agent (Explore) |
| 单点搜索 | Bash (grep/find) |
| 复杂实现规划 | Agent (Plan) 或 EnterPlanMode |
| 需要用户选择 | AskUserQuestion |

### 3.3 并行执行
- **独立**的只读操作（Read、Grep、Glob、WebFetch）**必须**在同一消息中并行发起。
- 有依赖关系的操作串行，不要猜测未知值。

### 3.4 过程中的沟通
- 首次工具调用前用**一句话**说明接下来要做什么。
- 发现意外情况、改变方向、遇到阻塞时，**主动告知用户**。
- 不做内部思考的流水账；只输出对用户有用的进展。

### 3.5 遇到错误的处理
1. **先诊断根因**，不要用破坏性手段绕过（禁止 `--no-verify`、`reset --hard` 之类的规避）。
2. 可逆地尝试修复 → 验证 → 继续。
3. 修复失败 2 次以上 ⇒ 停下来向用户同步现状、列出选项。

### 3.6 风险操作的红线
以下操作**必须**用户显式授权后才执行：
- 删除文件/分支、`rm -rf`、`git reset --hard`、force push。
- 修改 CI/CD、共享配置、环境变量。
- 推送到远端、发布 PR/评论、发送外部消息。
- 安装/卸载依赖、修改 lock 文件。

---

## 四、验证阶段 (Verification)

### 4.1 每个子任务完成后
- 对照该子任务的"完成条件"逐条核对。
- 只有**完全满足**才标 `completed`；有任何残缺保持 `in_progress` 并新建后续 todo。

### 4.2 整体任务完成前的检查清单
- [ ] 所有用户显式要求都已交付
- [ ] 代码可通过类型检查 / lint / 测试（如适用）
- [ ] 新代码符合项目风格与规则
- [ ] 无遗留的调试代码、console.log、TODO 占位
- [ ] 无多余的改动（不相关文件未被动）
- [ ] 产物位置、命名符合要求
- [ ] 对用户可见的变化已在回复中说明

### 4.3 UI/前端任务额外检查
- 在浏览器中实际使用一次被修改的功能
- 覆盖主路径和关键边界
- 关注是否引入其它功能的回归
- 无法实机验证时**明确告知**用户，不虚报完成

### 4.4 信任但复核
- Agent 子代理的"已完成"汇报只代表其意图，不等于事实。
- 涉及实际写入/修改的，**必须**用 Read / git diff 复核真实变更。

---

## 五、收尾阶段 (Closure)

### 5.1 回复格式
- 1–2 句话说明**改动了什么**与**下一步是什么**。
- 引用关键文件使用 `[filename](path)` 或 `[filename:line](path#Lline)`。
- 不写流水账、不重复 diff 内容。

### 5.2 记忆与持久化
- 用户偏好、项目背景、反馈意见 ⇒ 按类型写入 memory。
- 临时状态、当前会话上下文 ⇒ 不要入 memory。
- 指令中要求"持久化/永久记住/以后都这样" ⇒ 必须 memory 或 hooks，不能只口头应答。

### 5.3 未完成事项
- 任何未完成、未验证、跳过的部分，**必须在回复中显式说明**。
- 不使用"应该可以工作"的含糊话术；未测试就说未测试。

---

## 六、元规则 (Meta Rules)

1. **范围纪律**：用户授权的范围 = 本次可执行的范围，不扩张。
2. **诚实优先**：不确定、未验证、未测试的一律如实说明。
3. **最小改动**：能改 1 行不改 10 行，能不新建文件就不新建。
4. **可逆性偏好**：多个方案同效时选更可逆的那个。
5. **用户节奏**：探索性问题短答 + 推荐方案 + 主要权衡；不直接落地代码，等确认。
6. **规范自举**：本规范本身可被用户要求修改；修改后立即按新版本执行。

---

## 七、快速自检口诀

> **理解 → 拆解 → 依赖 → 执行 → 验证 → 汇报**
> 每步问自己：
> - 我真的明白用户要什么了吗？
> - 这一步最小且可验证吗？
> - 有没有更可逆的做法？
> - 我验证过了吗？
> - 我说的和我做的一致吗？
