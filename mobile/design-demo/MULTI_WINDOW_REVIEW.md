# 多窗口展示 Demo

## 当前审查版：保守细调（2026-09-17）

入口改为 [conservative.html](conservative.html)。下方为旧版提案记录，
不能作为当前实施范围。当前只做独立 HTML 原型，未修改 Flutter。
保留实机截图中的顶部入口、两排选择器、折叠手柄和输入框结构；
不增加搜索、全部 Agent 入口、大型模式切换条或终端预览。
默认展开；网页向下阅读折叠仅示意现有 Flutter 行为，正式落地复用原逻辑。
顶部菜单、配置、附件等仅占位，不冒充真实功能实现。

浏览器自审通过：终端往返保持展开/折叠状态、草稿、对话阅读位置；
两排标签点击高度48px；320px宽无整体横向溢出。静态终端与模拟消息
不证明原生键盘、真实事件刷新或无障碍已通过。历史VM截图仅作结构
参照，不能用不同内容计算空间收益。待用户审查后再决定正式改动。

基线来自 emulator-5554 的 CCB Mobile 实际截图：两排窗口/Agent 标签、
工作状态颜色、自动折叠和 Terminal 双模式均保留。本 Demo 只提出细节
改进：窗口标签显示完整语义，Agent 限定当前窗口，全部 Agent 作为分组
面板入口，折叠和终端目标保持一致。

自审：窗口和 Agent 两排适合当前数量；全部 Agent 入口直接按现有窗口显示
分组列表，不增加搜索。当前原型已有窗口记忆、分组列表、折叠、
双模式和隔离草稿交互，未验证原生键盘、
无障碍、终端 ANSI 渲染和真实状态事件。未改 Flutter 正式代码。

## 对话优先修订

根据用户反馈取消整行模式切换和独立收起手柄，恢复顶部小型终端
按钮。标题下的目标摘要负责展开/收起两排列表；输入区压成一行。
不显示常驻终端预览，折叠后仍保留窗口/Agent/状态摘要。
820px 高原型实测消息区域：折叠648px，展开566px。320px 宽无整体
横向溢出，模式切换保持目标与草稿。标签34px为视觉密度提案，正式
Flutter 实现需要另验触控命中区域。自动折叠示意未模拟原生输入法。
