# Useful Claude Skills

Optional Claude skill bundles for users to copy into a Claude provider or
managed agent home.

These skills are intentionally not wired into `install.sh` or `install.ps1`.
They should be installed explicitly by users who want them.

Current skills:

- `plan-tree`
