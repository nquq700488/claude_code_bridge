# PR353 custom-provider env key follow-up

Date: 2026-09-17

Owner authorized merging PR353 before applying the compatibility fix. GitHub
merge commit: `a7a0d3eddcc28735cee4729e2bc468c4f6dd5d7a`. Integrated origin/main
into the existing local branch without overwriting the mobile work.

PR353 unconditionally set `env_key = "OPENAI_API_KEY"` for every explicit
custom URL. Codex 0.154.0 reproduced `Missing environment variable` for a URL
without credentials; the same configuration without env_key attempted HTTP.

Follow-up: bind env_key only for an explicit profile key or a nonblank ambient
key when API inheritance is allowed. This mirrors the launcher's credential
availability. Do not read auth.json into environment variables or mutate the
source Provider; no-key local servers remain usable.

Verification:

- Six parameterized materialization cases: explicit key, explicit precedence,
  inherited key, disabled inheritance, missing key, whitespace-only key.
  Checks source config/auth immutability and the generated provider table.
- Related provider profiles, v2 launch and v3 loader suites: 356 passed.
- Real Codex 0.154.0 with isolated temporary CODEX_HOME and loopback HTTP
  recorder: explicit and inherited dummy keys sent the expected Bearer header;
  no-key and blocked inheritance sent HTTP with no Authorization header.
  No production endpoint or real credential was used. Recorder returned 401;
  this proves request-header behavior, not model generation success.
- `git diff --check` passed. No platform-specific implementation added.

Remote PR is merged; the compatibility follow-up is local until explicitly
pushed/integrated into main. No release was performed.
