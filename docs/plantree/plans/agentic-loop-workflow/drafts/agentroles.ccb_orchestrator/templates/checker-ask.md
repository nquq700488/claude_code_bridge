# Node Review Packet Reference

Historical filename retained for compatibility. The orchestrator declares
review obligations in the candidate; it does not submit a reviewer ask.

Exact node workspace/tree evidence:

Base/head commits:

Changed paths:

Worker result ref:

Acceptance refs:

Verification refs:

Check plan:

Fallback/degradation audit:

Return one of: `pass`, `rework_required`, `blocked`, `non_converged`.

The controller binds a concrete code reviewer after worker evidence is ready.
