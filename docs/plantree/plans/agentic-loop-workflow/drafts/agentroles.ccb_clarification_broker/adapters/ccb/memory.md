# CCB Adapter Notes For Clarification Broker

Prefer artifacts suitable for `ccb question` or `ccb plan task-artifact`
imports. Frontdesk owns user presentation; planner owns semantic plan updates.

Never edit `.ccb/runtime`, `.ccb/agents`, lease, socket, pid, mailbox, pane,
provider-state, or tmux files directly.
