# v8.6.0-beta.2 implementation record

Date: 2026-08-11

## Delta from beta.1

- Removed the nonexistent `commands/` directory from the Windows artifact
  payload allowlist.
- Added a regression test that every default payload directory exists in the
  repository before the native runner attempts an export.
- Rebound a real auto-selected `TmuxBackend` to the authoritative project
  socket. PR #293 had reused the process-global terminal backend cache, so the
  namespace could be created on an ambient tmux server and then accessed
  through a nonexistent project socket.
- Updated the Herdr namespace helper fake to match the current
  `provider_kind` identity contract.
- Preserved the failed `v8.6.0-beta.1` tag and its Actions evidence.

## Evidence status

- Exact Windows workflow Python gate: 290 passed with one environment skip.
- Broader Windows/Herdr gate: 416 passed with one environment skip.
- Project namespace regressions: 69 passed.
- Cross-platform lifecycle smoke: 8 passed, including the previously failing
  full project lifecycle.
- Deterministic merge-regression gates: 218 passed.
- Restored reconnect coverage: 67 tests and four subtests passed.
- Payload-copy, version identity, Python compile, workflow YAML, shell syntax,
  release-note, and diff-integrity gates: passed.
- Native Windows Python and Rust tests: passed.
- Native PE and ZIP artifact build: passed; the stale payload-directory defect
  from beta.1 was fixed.
- PowerShell archive installation: failed because `Confirm-HerdrReady` ignored
  `-Yes` and `CCB_INSTALL_ASSUME_YES=1`, treated the hosted runner as
  interactive, and called `Read-Host` with no input stream.
- Installed `ccb.exe` smoke and GitHub prerelease creation: correctly skipped.

## Immutable candidate result

- Tag: `v8.6.0-beta.2`
- Commit: `8b0d56d904f57411ed47bb41c3236d7a26daeb48`
- Actions run: `31517729365`
- Resolution: preserve this tag and publish the installer fix as
  `v8.6.0-beta.3`.

## Non-goals

- No npm, Linux, macOS, sidebar, or Android publication.
- No tag movement or replacement of `v8.6.0-beta.1`.
- No signed or fully self-contained distribution claim.
