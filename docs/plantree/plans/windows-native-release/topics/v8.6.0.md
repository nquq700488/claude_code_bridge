# v8.6.0 implementation record

Date: 2026-08-12
Status: published

## Scope

- Promote Python, npm, mobile, Windows launcher, projection, and workflow
  identity from `8.6.0-beta.3` to stable `8.6.0`.
- Publish the existing Linux, macOS, Android, Sidebar, and npm products.
- Attach the isolated Windows x64 ZIP to the stable GitHub Release while
  retaining beta support semantics.
- Include Pi native-session recovery and the project-scoped tmux socket fix
  accumulated since `v8.5.7`.

## Evidence entering final gates

- `v8.6.0-beta.3` Windows Server 2022 workflow passed native Python/Rust tests,
  PE/ZIP build, PowerShell archive installation, installed launcher smoke,
  upload, and downloaded-asset SHA256 verification.
- Cross Platform and CCBD real-platform workflows passed on the beta.3 source
  commit for Ubuntu, macOS, and WSL.
- Stable publication remains gated on synchronized version checks, bilingual
  release-note validation, local regression suites, package dry-run, immutable
  tag preflight, and post-publication asset/registry verification.

## Support boundary

`v8.6.0` is a stable CCB version. The Windows ZIP remains beta and does not
claim real GUI/provider qualification beyond the recorded Windows runner gates.

## Publication record

- Immutable tag `v8.6.0` points to release commit
  `00ccfb5c4f80fadb2af1a0fd939f8b1721804b13`.
- GitHub Release:
  `https://github.com/SeemSeam/claude_codex_bridge/releases/tag/v8.6.0`;
  verified as the non-draft, non-prerelease Latest release.
- Release Artifacts run `31533104172` passed for Linux, macOS, Android, and
  unified checksums.
- Native Windows Release run `31533104206` passed native tests, PE/ZIP build,
  PowerShell archive install, installed launcher smoke, and publication.
- Sidebar run `31533104173` and npm Trusted Publishing run `31533104226`
  passed.
- Main-commit Cross-Platform run `31531696529` and CCBD Real Platform run
  `31531696404` passed before tagging.
- All ten GitHub assets were downloaded again. `SHA256SUMS`, the Windows ZIP
  sidecar, and the Sidebar sidecar verified successfully; the Windows manifest
  reports version `8.6.0` with support tier `beta`.
- npm reports `@seemseam/ccb@8.6.0` as `latest`; a clean npm installation
  returned CLI version `v8.6.0` and passed its help smoke.

## Local gate record

- Windows/Herdr release surface: `388 passed, 1 skipped`.
- Stable workflow, mobile update, and Pi session recovery: `221 passed`.
- Reconnect and lifecycle: `146 passed`.
- Terminal runtime/public import surface: `118 passed`.
- Version identity, workflow YAML, publication-shell syntax, Python compile,
  bilingual notes, diff checks, and npm `8.6.0` dry-run passed.
- The complete non-provider-blackbox suite reported `6621 passed, 3 skipped,
  13 failed`. Eight deterministic failures reproduce unchanged at the
  pre-release base commit `41c880f`; the remaining five pass in a clean process
  and are suite-order pollution. No failure is introduced by the `v8.6.0`
  release delta. The existing baseline test debt remains separate from this
  release and is not represented as Windows qualification evidence.
