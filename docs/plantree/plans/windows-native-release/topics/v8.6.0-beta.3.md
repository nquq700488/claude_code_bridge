# v8.6.0-beta.3 implementation record

Date: 2026-08-11

## Delta from beta.2

- `Confirm-HerdrReady` now treats `-Yes` and
  `CCB_INSTALL_ASSUME_YES=1` as explicit acknowledgement of the same guarded
  continuation offered by the interactive missing-Herdr prompt.
- The installer still warns that normal CCB startup requires Herdr. The CI
  lane may continue without Herdr only because it exercises the documented
  safe `--print-version` and `--help` surfaces.
- The fallback interactive prompt casts `Read-Host` to a string before
  trimming, so redirected input cannot cause a null-method exception.
- Preserved the failed `v8.6.0-beta.2` tag and Actions evidence.

## Evidence status

- Static installer acknowledgement regression: passed.
- Exact Windows workflow Python gate: 291 passed with one environment skip.
- Broader Windows/Herdr gate: 417 passed with one environment skip.
- Project namespace regressions: 69 passed.
- Reconnect coverage: 67 passed with four subtests.
- Payload-copy, version identity, Python compile, workflow YAML, shell syntax,
  release-note, and diff-integrity gates: passed.
- Cross-Platform Compatibility run `31516790689`: passed on Ubuntu and macOS.
- CCBD Real Platform Smoke run `31516787774`: passed on macOS and WSL,
  including lifecycle, communication, soak, fastpath, and cleanup gates.
- Native Windows Python/Rust tests, PE/ZIP build, PowerShell archive install,
  installed `ccb.exe --print-version/--help` smoke, and artifact upload: passed
  in Actions run `31518445728`.
- GitHub prerelease:
  `https://github.com/SeemSeam/claude_codex_bridge/releases/tag/v8.6.0-beta.3`.
- Published assets: `ccb-windows-x86_64.zip` (23,406,628 bytes) and
  `ccb-windows-x86_64.zip.sha256`.
- Post-publication download verification: `sha256sum -c` passed.

## Published candidate

- Tag: `v8.6.0-beta.3`
- Commit: `4bc2e8ceb1d9b103932634591ba8d6ed29a225ce`
- Draft: no.
- Prerelease: yes.
- Stable npm, Linux/macOS artifact, Sidebar, and Android release workflows:
  skipped as designed.

## Non-goals

- No npm, Linux, macOS, sidebar, or Android publication.
- No tag movement or replacement of beta.1 or beta.2.
- No claim that ordinary provider startup works without Herdr.
- No signed or fully self-contained distribution claim.
