# PR340-344 Review And Repairs

Date: 2026-09-09
Role: evidence
Status: partial; local repairs verified, merge uncommitted
Authority: [Core PR requirements](../test-and-release-gates.md#core-pr-maintenance-requirements)

## Local Repairs

Local base: `ee6d75e02`. Reviewed upstream through `0ec928a22` (PR340-342)
is merged into the working tree without a merge commit. Existing local
maintenance documents are preserved. No push or release was performed.

- PR340: generated launchers unset credential aliases. Prepending recovered
  credentials therefore erased them before Claude started. Recovery now
  inserts credentials after leading environment setup statements, preserving
  the original command text and shell template. Tests execute generated
  commands with synthetic API key/token values, including quotes and semicolons.
- PR341: continuation detection treated option values as subcommands.
  The scanner now skips recognized option values and stops at the first
  positional token. Tests cover long/short/attached option values, real fork,
  resume rewriting, and the end-of-options delimiter.
- PR342: no blocking finding in the reviewed changes.

Verification: Claude launcher, session fields, session selection and Codex
start-command suites: 56 passed. No real credentials or external Provider
configuration were changed by these tests.

## New Upstream Review

Reviewed latest upstream `61890b0aa` in an independent worktree.
PR343 and PR344 are not merged into the local candidate.

- PR343 (`d67686e2`), P1: `start-server` and `set-option -g exit-empty off`
  remain separate tmux invocations. An empty server can exit between them.
  Independent tmux 3.6 socket reproduction: start returns 0, subsequent option
  setting returns 1 with "no server running". Set startup policy in the same
  tmux command queue or create the session atomically; add real tmux coverage.
- PR344 (`61890b0aa`), authority blocker: the generated managed preference
  points both DefaultKeychain and DLDBSearchList to the user's login keychain.
  Creating a private plist does not make the referenced database private or
  read-only. The tests check plist content and missing symlinks, not native
  Claude write/refresh/logout effects on external authority. No macOS native
  verification was available; actual external credential corruption is not
  claimed. Require isolated storage or evidence enforcing the accepted
  one-way authority boundary before accepting this contract relaxation.

Upstream namespace backend and provider-profile suites: 191 passed.
Windows isolation gate for the combined new range fails on
`windows-platform-check`: its pattern matches any `platform.system()` call,
including the added Darwin check. This is a classification false positive,
not evidence of a new Windows dependency. Do not bypass or weaken the gate
inside the functional PR; any policy correction needs separate review.

## Remaining Work

- Commit the reviewed local merge and repairs after maintenance review.
- Keep native macOS verification and isolation-policy correction explicit;
  passing mocked unit tests does not establish those properties.

## Follow-Up Disposition

On 2026-09-09 the owner authorized repairing or rejecting the remaining PRs.

- PR343: implemented a corrected equivalent locally. `prepare_server` sends
  `start-server ; set-option -g exit-empty off` in one tmux command queue.
  It does not add a permanent policy change to unrelated existing sessions.
  The new real-tmux regression checks the empty server through a separate
  client before creating a session, verifies session availability, and always
  kills its private test server. Existing transient-retry coverage now retries
  the complete command queue.
- PR344: rejected for this local candidate. No generated DefaultKeychain or
  DLDBSearchList pointing to the user's login database is introduced. The
  original isolation implementation and both affected contracts are unchanged
  from the local base. This records local rejection only: GitHub already merged
  the PR, and no remote revert, comment, or release operation was performed.
- Final combined regression: namespace backend, provider profiles, Claude
  launcher, Claude session fields, and Codex command parsing: **240 passed**,
  including the real tmux test. `git diff --check` passes.

Publication preparation supersedes the earlier local-only status: integration
commits `f102a0e3c` and `64e19fddd` preserve upstream ancestry and the corrected
tmux startup. Commit `21ffbc310` explicitly reverts PR344. The owner authorized
the GitHub rejection comment, main push, and v8.6.16 publication. Release
metadata is maintained in a separate release commit; remote verification is
required before describing the version as published.

v8.6.16 pre-publication validation in the clean release worktree: 453 focused
Python tests passed (provider recovery, namespace, Mobile gateway, npm runner,
and release builders); Flutter full suite 814 passed / 1 skipped; Flutter
analyze reported no issues. Version consistency, bilingual notes and npm pack
dry-run passed. The GitHub rejection comment is recorded at
https://github.com/SeemSeam/claude_codex_bridge/pull/344#issuecomment-5598648523.
